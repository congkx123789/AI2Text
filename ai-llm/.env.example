# Force PyTorch-only
TRANSFORMERS_NO_TF=1

# ASR (Whisper) — use local copy + CPU
ASR_MODEL=./models/base/whisper-small
ASR_DEVICE=cpu
ASR_COMPUTE=int8

# RAG models — use local copies
EMBEDDING_MODEL=./models/base/embedder
RERANKER_MODEL=./models/base/reranker

# LLM — use local copy for offline, OR a tiny test model to rule things out
# GEN_MODEL=./models/base/generator
GEN_MODEL=sshleifer/tiny-gpt2
GEN_MAX_TOKENS=128
